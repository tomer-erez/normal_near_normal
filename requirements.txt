# PyTorch — install with CUDA support separately before the rest:
#   conda install pytorch torchvision pytorch-cuda=12.1 -c pytorch -c nvidia
# or via pip:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# torch
# torchvision

# Data processing
numpy
pandas
Pillow

# HuggingFace ecosystem (LoRA + CXR-CLIP text encoder + BiomedCLIP)
transformers
peft
huggingface_hub

# OpenCLIP dependencies
# Install the local submodule first: pip install -e open_clip/
ftfy
regex
tqdm

# Evaluation & plotting (run_all_evals.py)
matplotlib
seaborn

# CLIP retrieval pipeline (build_baseline.py)
clip-retrieval
