================================================================================
 MIMIC-CXR CLIP FINE-TUNING — FULL GUIDE
================================================================================

Shorthands (set in your shell or substitute inline):
  PAIRED_DIR=./eval_outputs/baseline_output_official_test/paired_data
  TEST_CSV=cxr_data/mimic_cxr_official_test.csv
  BIOMEDCLIP=hf-hub:microsoft/BiomedCLIP-PubMedBERT_256-vit_base_patch16_224

--image-dir, --train-csv, and --output-dir are hardcoded in train_lora.py;
omit them from all commands below unless overriding.


================================================================================
 RUNNING IN THE BACKGROUND  (run.sh)
================================================================================

Prefix any train_lora.py or run_all_evals.py command with "bash run.sh":

  bash run.sh python train_lora.py --base-model ViT-B-32 --pretrained openai \
      --match-mode negative_aware --loss clip \
      --negative-weight 0.5 --negative-margin 0.0 \
      --batch-size 128 --epochs 10 \
      --wandb-project mimic-cxr-clip --wandb-run-name exp1d_negaware_clip

The script derives the log filename from --wandb-run-name and writes:
  logs/<name>.log   — all stdout/stderr
  logs/<name>.pid   — PID for easy killing

After launch it prints:
  PID 12345
    follow : tail -f logs/exp1d_negaware_clip.log
    kill   : kill $(cat logs/exp1d_negaware_clip.pid)

Works with torchrun too:
  bash run.sh torchrun --nproc_per_node=4 train_lora.py ...


================================================================================
 WEIGHTS & BIASES (W&B) TRACKING
================================================================================

Both scripts support W&B via:
  --wandb-project  PROJECT   required to enable logging
  --wandb-run-name NAME      display name for the run
  --wandb-entity   ENTITY    team/user (uses your default if omitted)

Training logs: train/loss_step (per step), train/loss, val/loss, train/lr,
               train/logit_scale (per epoch).
Eval logs:     eval/macro_summary table, per-model scalar metrics, all PNGs.

Use the same --wandb-project for training and eval to compare runs.


================================================================================
 FULL PIPELINE
================================================================================

--- Step 0 — Build train/test CSVs (run once) ---

python baseline_eval/create_train_test_sets.py \
    --csv       cxr_data/all_txt_data_and_labels.csv \
    --split     cxr_data/images/mimic_cxr_jpg_images_from_google_cloud/mimic-cxr-jpg-2.1.0.physionet.org/mimic-cxr-2.0.0-split.csv.gz \
    --out_train cxr_data/mimic_cxr_train.csv \
    --out_test  cxr_data/mimic_cxr_official_test.csv

Produces ~227k train rows and 5,159 official test rows.


--- Step 1 — Build the image gallery (run once) ---

python baseline_eval/build_baseline.py \
    --csv        cxr_data/mimic_cxr_official_test.csv \
    --output_dir ./eval_outputs/baseline_output_official_test

Creates symlinks + paired .txt files for 5,159 test images in
eval_outputs/baseline_output_official_test/paired_data/.


--- Step 2 — Evaluate all models and compare ---

python baseline_eval/run_all_evals.py \
    --paired_dir $PAIRED_DIR --csv $TEST_CSV \
    --output_dir ./eval_outputs/all_results --batch_size 64 \
    --wandb-project mimic-cxr-clip --wandb-run-name all_results

Add --skip_existing to skip models already evaluated. Produces per-model
result CSVs, summary_macro.csv, and plots/ in --output_dir.


================================================================================
 TRAINING QUICK REFERENCE
================================================================================

# Smoke test — verify pipeline end-to-end (tiny subset)
python train_lora.py --base-model ViT-S-32-alt \
    --batch-size 4 --epochs 1 --max-samples 8 \
    --wandb-project mimic-cxr-clip --wandb-run-name smoke

# Resume from checkpoint
python train_lora.py ... --resume ./experiments/lora_vitb32/epoch_5_adapter.pt

# Multi-GPU — standard mode only; for label-aware use larger --batch-size instead
CUDA_VISIBLE_DEVICES=0,1,2,3 torchrun --nproc_per_node=4 train_lora.py \
    --base-model ViT-B-32 --pretrained openai \
    --match-mode standard --batch-size 64 --epochs 10 \
    --wandb-project mimic-cxr-clip --wandb-run-name exp_4gpu

Outputs per run:
  experiments/<name>/epoch_N_adapter.pt   LoRA adapter (~11 MB)
  experiments/<name>/final_merged.pt      Merged model for eval


================================================================================
 EVAL QUICK REFERENCE  (single model)
================================================================================

python baseline_eval/eval_model.py \
    --model_type finetuned --name <name> \
    --finetuned_base_model ViT-B-32 --finetuned_pretrained openai \
    --finetuned_checkpoint ./experiments/<name>/final_merged.pt \
    --paired_dir $PAIRED_DIR --csv $TEST_CSV

# Baselines (no fine-tuning)
python baseline_eval/eval_model.py --model_type vanilla_clip \
    --paired_dir $PAIRED_DIR --csv $TEST_CSV
python baseline_eval/eval_model.py --model_type biomedclip \
    --paired_dir $PAIRED_DIR --csv $TEST_CSV

# Limit to a specific query mode (default: all)
... --query_mode negative


================================================================================
 ADDING A FINE-TUNED MODEL TO run_all_evals.py
================================================================================

Edit the MODELS list in baseline_eval/run_all_evals.py:

  {
      "name": "lora_vitb32",
      "model_type": "finetuned",
      "finetuned_base_model": "ViT-B-32",
      "finetuned_pretrained": "openai",
      "finetuned_checkpoint": "experiments/lora_vitb32/final_merged.pt",
  },


================================================================================
 LABEL ENCODING
================================================================================

  CSV value  Meaning                          Tensor value
  ---------  -------------------------------- ------------
      1      Pathology positively mentioned   1.0  (positive)
      0      Pathology explicitly ruled out   -1.0 (negative)
     NaN     Label not mentioned in report    -1.0 (negative — same as 0)
     -1      Ambiguous / uncertain mention    0.0  (ignored)

NaN and 0 are both treated as -1.0: absence of evidence = evidence of absence.
Only -1 (uncertain language like "possible" or "cannot exclude") is ignored.
Consequence: if sample A has Edema=1 and B has Edema=NaN, they are flagged as
a conflict in negative_aware mode.


================================================================================
 KNOB 1 — CAPTION MODE  (--caption-mode)
================================================================================

  single   One random positive label         "atelectasis"
  pair     Two random positive labels        "fracture and edema"
  both     Randomly single or pair           (DEFAULT, 75%/25%)

Keep both (default) — eval uses both single and pair query types.


================================================================================
 KNOB 2 — MATCH MODE  (--match-mode)
================================================================================

Controls which (image, text) pairs count as positives. Standard CLIP uses only
the diagonal — harmful in multi-label data where two images with identical
findings should attract each other.

  standard       Diagonal-only. Uses ClipLoss with distributed all-gather.
  single_label   Positive if >= 1 shared label. Soft target matrix.
  two_label      Positive if >= 2 shared labels. Pair with --caption-mode pair.
  negative_aware single_label + hinge repulsion for conflict pairs
                 (one sample label=1, other label=0/NaN).
                 Extra flags: --negative-weight lambda (default 0.5)
                              --negative-margin tau   (default 0.0)

Diagnostics logged at step 0 of each epoch (label-aware modes):
  avg_positives_per_sample  should be >1.0; if ~1.0 -> increase --batch-size
  diagonal_only (%)         keep <20%
  conflict_pairs (%)        5-15% is healthy for negative_aware


================================================================================
 KNOB 3 — LOSS VARIANT  (--loss)
================================================================================

  clip    (DEFAULT) Softmax multi-positive cross-entropy per row.
  siglip  Sigmoid binary BCE per pair — loss decoupled from batch size.

Both work with any --match-mode. Tuning lambda/tau for negative_aware:
  lambda = 0.25  conservative (use if training is unstable)
  lambda = 0.5   default balanced
  lambda = 1.0   aggressive repulsion
  tau = 0.0      penalise any positive conflict similarity (default)
  tau = 0.2      more aggressive


================================================================================
 KNOB 4 — QUERY MODE  (--query_mode, eval only)
================================================================================

  single    13 queries   "atelectasis"               label_A == 1
  pair      78 queries   "atelectasis and edema"     label_A == 1 AND label_B == 1
  negative  156 queries  "atelectasis and no edema"  label_A == 1 AND label_B == 0/NaN
  all       247 queries  all of the above            (DEFAULT)


================================================================================
 RECIPES (quick-reference)
================================================================================

  Recipe A — Baseline (vanilla CLIP, diagonal pairs)
    --match-mode standard --loss clip

  Recipe B — Soft positives (single label match, CLIP loss)
    --match-mode single_label

  Recipe C — Strict positives (two shared labels)
    --match-mode two_label --caption-mode pair

  Recipe D — Negative-aware CLIP
    --match-mode negative_aware --negative-weight 0.5 --negative-margin 0.0

  Recipe E — SigLIP loss, label-aware
    --loss siglip --match-mode single_label

  Recipe F — SigLIP + negative-aware (strongest repulsion)
    --loss siglip --match-mode negative_aware --negative-weight 0.5 --negative-margin 0.0


================================================================================
 DESIGN NOTES
================================================================================

Label-aware modes operate on the LOCAL batch only. For multi-GPU, positive
mining is per-GPU; standard mode retains full all-gather. For label-aware +
multi-GPU, increase --batch-size rather than using torchrun.

Conflict detection covers CSV 0 AND NaN (both -> -1.0). Uncertain labels
(CSV -1 -> 0.0) are fully ignored in both positive matching and conflict
detection.


================================================================================
 EXPERIMENTS
================================================================================


────────────────────────────────────────────────────────────────────────────────
IDEA 1 — stronger, larger models
What: Test whether more powerful models and loss functions improve retrieval.
      Each recipe below is a full training + eval pipeline. Run all to
      compare side-by-side in W&B and summary_macro.csv.
________________________________________________________________________________

python train_lora.py --base-model ViT-L-14 --pretrained openai \
    --wandb-project mimic-cxr-clip --wandb-run-name exp1b_single_clip


────────────────────────────────────────────────────────────────────────────────
IDEA 1a — Vanilla CLIP baseline (diagonal pairs, softmax loss)
What: Standard CLIP fine-tuning with no multi-label awareness. Establishes
      the baseline retrieval performance.
────────────────────────────────────────────────────────────────────────────────

python train_lora.py --base-model ViT-B-32 --pretrained openai \
    --match-mode standard --loss clip --batch-size 128 --epochs 10 \
    --wandb-project mimic-cxr-clip --wandb-run-name exp1a_standard_clip

python baseline_eval/eval_model.py \
    --model_type finetuned --name exp1a_standard_clip \
    --finetuned_base_model ViT-B-32 --finetuned_pretrained openai \
    --finetuned_checkpoint ./experiments/exp1a_standard_clip/final_merged.pt \
    --paired_dir $PAIRED_DIR --csv $TEST_CSV


────────────────────────────────────────────────────────────────────────────────
IDEA 1b — Single-label soft positives (CLIP loss)
What: Two images are positives if they share >= 1 CheXpert label. Fixes the
      false-negative problem in standard CLIP for multi-label data.
────────────────────────────────────────────────────────────────────────────────

python train_lora.py --base-model ViT-B-32 --pretrained openai \
    --match-mode single_label --loss clip --batch-size 128 --epochs 10 \
    --wandb-project mimic-cxr-clip --wandb-run-name exp1b_single_clip

python baseline_eval/eval_model.py \
    --model_type finetuned --name exp1b_single_clip \
    --finetuned_base_model ViT-B-32 --finetuned_pretrained openai \
    --finetuned_checkpoint ./experiments/exp1b_single_clip/final_merged.pt \
    --paired_dir $PAIRED_DIR --csv $TEST_CSV


────────────────────────────────────────────────────────────────────────────────
IDEA 1c — Two-label strict positives + pair captions (CLIP loss)
What: Two images are positives only if they share >= 2 labels. Pair with
      two-label captions to align training and eval query format.
────────────────────────────────────────────────────────────────────────────────

python train_lora.py --base-model ViT-B-32 --pretrained openai \
    --match-mode two_label --caption-mode pair --loss clip \
    --batch-size 128 --epochs 10 \
    --wandb-project mimic-cxr-clip --wandb-run-name exp1c_two_clip

python baseline_eval/eval_model.py \
    --model_type finetuned --name exp1c_two_clip \
    --finetuned_base_model ViT-B-32 --finetuned_pretrained openai \
    --finetuned_checkpoint ./experiments/exp1c_two_clip/final_merged.pt \
    --paired_dir $PAIRED_DIR --csv $TEST_CSV


────────────────────────────────────────────────────────────────────────────────
IDEA 1d — Negative-aware CLIP (attract shared labels, repel conflicting)
What: single_label positives + hinge repulsion for conflict pairs. Tests
      whether explicit repulsion improves "yes A and no B" query retrieval.
────────────────────────────────────────────────────────────────────────────────

python train_lora.py --base-model ViT-B-32 --pretrained openai \
    --match-mode negative_aware --loss clip \
    --negative-weight 0.5 --negative-margin 0.0 \
    --batch-size 128 --epochs 10 \
    --wandb-project mimic-cxr-clip --wandb-run-name exp1d_negaware_clip

python baseline_eval/eval_model.py \
    --model_type finetuned --name exp1d_negaware_clip \
    --finetuned_base_model ViT-B-32 --finetuned_pretrained openai \
    --finetuned_checkpoint ./experiments/exp1d_negaware_clip/final_merged.pt \
    --paired_dir $PAIRED_DIR --csv $TEST_CSV


────────────────────────────────────────────────────────────────────────────────
IDEA 1e — Single-label SigLIP loss
What: Per-pair sigmoid BCE instead of softmax. Each (image, text) pair is an
      independent binary decision — decouples loss magnitude from batch size.
────────────────────────────────────────────────────────────────────────────────

python train_lora.py --base-model ViT-B-32 --pretrained openai \
    --match-mode single_label --loss siglip --batch-size 128 --epochs 10 \
    --wandb-project mimic-cxr-clip --wandb-run-name exp1e_single_siglip

python baseline_eval/eval_model.py \
    --model_type finetuned --name exp1e_single_siglip \
    --finetuned_base_model ViT-B-32 --finetuned_pretrained openai \
    --finetuned_checkpoint ./experiments/exp1e_single_siglip/final_merged.pt \
    --paired_dir $PAIRED_DIR --csv $TEST_CSV


────────────────────────────────────────────────────────────────────────────────
IDEA 1f — Negative-aware SigLIP (strongest repulsion signal)
What: SigLIP + negative-aware. Conflicting pairs get target=-1 in sigmoid
      (built-in repulsion) AND an extra hinge penalty.
────────────────────────────────────────────────────────────────────────────────

python train_lora.py --base-model ViT-B-32 --pretrained openai \
    --match-mode negative_aware --loss siglip \
    --negative-weight 0.5 --negative-margin 0.0 \
    --batch-size 128 --epochs 10 \
    --wandb-project mimic-cxr-clip --wandb-run-name exp1f_negaware_siglip

python baseline_eval/eval_model.py \
    --model_type finetuned --name exp1f_negaware_siglip \
    --finetuned_base_model ViT-B-32 --finetuned_pretrained openai \
    --finetuned_checkpoint ./experiments/exp1f_negaware_siglip/final_merged.pt \
    --paired_dir $PAIRED_DIR --csv $TEST_CSV


────────────────────────────────────────────────────────────────────────────────
IDEA 1g — Frozen CXR-CLIP image encoder + fine-tuned OpenCLIP text encoder
What: Keep a CXR-CLIP image encoder frozen (pre-trained on biomedical data)
      and fine-tune a ViT-B-32 text encoder to align with it. Both project
      to 512-dim — no architecture changes needed. LoRA applies to text only.
      Checkpoints in valid_pretrained_models_to_try/:
        r50_mc.pt   — ResNet50 image encoder
        swint_mc.pt — Swin-T image encoder
────────────────────────────────────────────────────────────────────────────────

# r50_mc + single_label CLIP
python train_lora.py --base-model ViT-B-32 --pretrained openai \
    --cxrclip-checkpoint valid_pretrained_models_to_try/r50_mc.pt \
    --match-mode single_label --loss clip --batch-size 128 --epochs 10 \
    --wandb-project mimic-cxr-clip --wandb-run-name exp8_r50_mc_single_clip

# r50_mc + negative-aware CLIP
python train_lora.py --base-model ViT-B-32 --pretrained openai \
    --cxrclip-checkpoint valid_pretrained_models_to_try/r50_mc.pt \
    --match-mode negative_aware --loss clip \
    --negative-weight 0.5 --negative-margin 0.0 \
    --batch-size 128 --epochs 10 \
    --wandb-project mimic-cxr-clip --wandb-run-name exp8_r50_mc_negaware_clip

# swint_mc + single_label CLIP
python train_lora.py --base-model ViT-B-32 --pretrained openai \
    --cxrclip-checkpoint valid_pretrained_models_to_try/swint_mc.pt \
    --match-mode single_label --loss clip --batch-size 128 --epochs 10 \
    --wandb-project mimic-cxr-clip --wandb-run-name exp8_swint_mc_single_clip

# Eval (substitute name and matching checkpoints):
python baseline_eval/eval_model.py \
    --model_type cxrclip_hybrid --name exp8_r50_mc_single_clip \
    --cxrclip_image_checkpoint valid_pretrained_models_to_try/r50_mc.pt \
    --hybrid_merged_checkpoint ./experiments/exp8_r50_mc_single_clip/final_merged.pt \
    --hybrid_text_model ViT-B-32 --hybrid_text_pretrained openai \
    --paired_dir $PAIRED_DIR --csv $TEST_CSV

# run_all_evals.py MODELS entry for hybrid models:
#   {
#       "name": "exp8_r50_mc_single_clip",
#       "model_type": "cxrclip_hybrid",
#       "cxrclip_image_checkpoint": "valid_pretrained_models_to_try/r50_mc.pt",
#       "hybrid_text_model": "ViT-B-32",
#       "hybrid_text_pretrained": "openai",
#       "hybrid_merged_checkpoint": "experiments/exp8_r50_mc_single_clip/final_merged.pt",
#   },


────────────────────────────────────────────────────────────────────────────────
IDEA 2 — Batch size effect on label-aware mining
What: Label-aware modes need in-batch positive partners. If
      avg_positives_per_sample ~= 1.0 the batch is too small.
      Note: --grad-accum-steps does NOT increase the label matrix size.
────────────────────────────────────────────────────────────────────────────────

python train_lora.py --base-model ViT-B-32 --pretrained openai \
    --match-mode single_label --batch-size 32 --epochs 10 \
    --wandb-project mimic-cxr-clip --wandb-run-name exp2_bs32

python train_lora.py --base-model ViT-B-32 --pretrained openai \
    --match-mode single_label --batch-size 128 --epochs 10 \
    --wandb-project mimic-cxr-clip --wandb-run-name exp2_bs128

# Eval (substitute name and checkpoint for each):
python baseline_eval/eval_model.py \
    --model_type finetuned --name exp2_bs32 \
    --finetuned_base_model ViT-B-32 --finetuned_pretrained openai \
    --finetuned_checkpoint ./experiments/exp2_bs32/final_merged.pt \
    --paired_dir $PAIRED_DIR --csv $TEST_CSV


────────────────────────────────────────────────────────────────────────────────
IDEA 3 — LoRA encoder target ablation (image / text / both)
What: Does fine-tuning the image encoder, text encoder, or both matter most?
      Hypothesis: text encoder dominates as it shapes the query space.
────────────────────────────────────────────────────────────────────────────────

python train_lora.py --base-model ViT-B-32 --pretrained openai \
    --match-mode single_label --lora-target image --batch-size 128 --epochs 10 \
    --wandb-project mimic-cxr-clip --wandb-run-name exp3_image_only

python train_lora.py --base-model ViT-B-32 --pretrained openai \
    --match-mode single_label --lora-target text --batch-size 128 --epochs 10 \
    --wandb-project mimic-cxr-clip --wandb-run-name exp3_text_only

python train_lora.py --base-model ViT-B-32 --pretrained openai \
    --match-mode single_label --lora-target both --batch-size 128 --epochs 10 \
    --wandb-project mimic-cxr-clip --wandb-run-name exp3_both

# Eval (substitute name and checkpoint for each):
python baseline_eval/eval_model.py \
    --model_type finetuned --name exp3_image_only \
    --finetuned_base_model ViT-B-32 --finetuned_pretrained openai \
    --finetuned_checkpoint ./experiments/exp3_image_only/final_merged.pt \
    --paired_dir $PAIRED_DIR --csv $TEST_CSV


────────────────────────────────────────────────────────────────────────────────
IDEA 4 — LoRA rank ablation (r=4 / r=16 / r=64)
What: Higher rank = more capacity, more overfitting risk. Default r=16 adds
      ~1.9% trainable params (~2.9M / 154M). Rule: lora-alpha = 2 x lora-r.
────────────────────────────────────────────────────────────────────────────────

python train_lora.py --base-model ViT-B-32 --pretrained openai \
    --match-mode single_label --lora-r 4 --lora-alpha 8 \
    --batch-size 128 --epochs 10 \
    --wandb-project mimic-cxr-clip --wandb-run-name exp4_r4

python train_lora.py --base-model ViT-B-32 --pretrained openai \
    --match-mode single_label --lora-r 16 --lora-alpha 32 \
    --batch-size 128 --epochs 10 \
    --wandb-project mimic-cxr-clip --wandb-run-name exp4_r16

python train_lora.py --base-model ViT-B-32 --pretrained openai \
    --match-mode single_label --lora-r 64 --lora-alpha 128 \
    --batch-size 128 --epochs 10 \
    --wandb-project mimic-cxr-clip --wandb-run-name exp4_r64

# Eval (substitute name and checkpoint for each):
python baseline_eval/eval_model.py \
    --model_type finetuned --name exp4_r4 \
    --finetuned_base_model ViT-B-32 --finetuned_pretrained openai \
    --finetuned_checkpoint ./experiments/exp4_r4/final_merged.pt \
    --paired_dir $PAIRED_DIR --csv $TEST_CSV


────────────────────────────────────────────────────────────────────────────────
IDEA 5 — Repulsion strength ablation (lambda and tau tuning)
What: lambda controls how hard conflict pairs are pushed apart; tau is the
      cosine-sim threshold below which repulsion is skipped. Use
      --query_mode negative for the clearest eval signal.
────────────────────────────────────────────────────────────────────────────────

python train_lora.py --base-model ViT-B-32 --pretrained openai \
    --match-mode negative_aware --loss clip \
    --negative-weight 0.25 --negative-margin 0.0 \
    --batch-size 128 --epochs 10 \
    --wandb-project mimic-cxr-clip --wandb-run-name exp5_w025

python train_lora.py --base-model ViT-B-32 --pretrained openai \
    --match-mode negative_aware --loss clip \
    --negative-weight 0.5 --negative-margin 0.0 \
    --batch-size 128 --epochs 10 \
    --wandb-project mimic-cxr-clip --wandb-run-name exp5_w05

python train_lora.py --base-model ViT-B-32 --pretrained openai \
    --match-mode negative_aware --loss clip \
    --negative-weight 1.0 --negative-margin 0.0 \
    --batch-size 128 --epochs 10 \
    --wandb-project mimic-cxr-clip --wandb-run-name exp5_w10

python train_lora.py --base-model ViT-B-32 --pretrained openai \
    --match-mode negative_aware --loss clip \
    --negative-weight 0.5 --negative-margin 0.2 \
    --batch-size 128 --epochs 10 \
    --wandb-project mimic-cxr-clip --wandb-run-name exp5_margin02

# Eval on negative queries only (most informative for this ablation);
# repeat substituting exp5_w05, exp5_w10, exp5_margin02:
python baseline_eval/eval_model.py \
    --model_type finetuned --name exp5_w025 \
    --finetuned_base_model ViT-B-32 --finetuned_pretrained openai \
    --finetuned_checkpoint ./experiments/exp5_w025/final_merged.pt \
    --paired_dir $PAIRED_DIR --csv $TEST_CSV --query_mode negative


────────────────────────────────────────────────────────────────────────────────
IDEA 6 — BiomedCLIP base model
What: BiomedCLIP is pre-trained on biomedical image-text pairs (PMC). Likely
      stronger prior for CXR vocabulary. Needs expanded --lora-modules to
      cover HuggingFace transformer layers.
────────────────────────────────────────────────────────────────────────────────

python train_lora.py --base-model $BIOMEDCLIP \
    --match-mode standard --loss clip \
    --batch-size 32 --grad-accum-steps 4 --epochs 10 \
    --lora-modules "qkv,proj,fc1,fc2,query,key,value" \
    --wandb-project mimic-cxr-clip --wandb-run-name exp6_biomedclip_standard

python train_lora.py --base-model $BIOMEDCLIP \
    --match-mode negative_aware --loss clip \
    --negative-weight 0.5 --negative-margin 0.0 \
    --batch-size 32 --grad-accum-steps 4 --epochs 10 \
    --lora-modules "qkv,proj,fc1,fc2,query,key,value" \
    --wandb-project mimic-cxr-clip --wandb-run-name exp6_biomedclip_negaware

python baseline_eval/eval_model.py \
    --model_type finetuned --name exp6_biomedclip_standard \
    --finetuned_base_model $BIOMEDCLIP --finetuned_pretrained "" \
    --finetuned_checkpoint ./experiments/exp6_biomedclip_standard/final_merged.pt \
    --paired_dir $PAIRED_DIR --csv $TEST_CSV

python baseline_eval/eval_model.py \
    --model_type finetuned --name exp6_biomedclip_negaware \
    --finetuned_base_model $BIOMEDCLIP --finetuned_pretrained "" \
    --finetuned_checkpoint ./experiments/exp6_biomedclip_negaware/final_merged.pt \
    --paired_dir $PAIRED_DIR --csv $TEST_CSV


────────────────────────────────────────────────────────────────────────────────
IDEA 7 — Caption mode ablation (single / pair / both)
What: Does training with only single-label captions hurt pair query retrieval?
      Default "both" (75%/25%) should generalise best across all query types.
────────────────────────────────────────────────────────────────────────────────

python train_lora.py --base-model ViT-B-32 --pretrained openai \
    --match-mode single_label --caption-mode single \
    --batch-size 128 --epochs 10 \
    --wandb-project mimic-cxr-clip --wandb-run-name exp7_caption_single

python train_lora.py --base-model ViT-B-32 --pretrained openai \
    --match-mode two_label --caption-mode pair \
    --batch-size 128 --epochs 10 \
    --wandb-project mimic-cxr-clip --wandb-run-name exp7_caption_pair

python train_lora.py --base-model ViT-B-32 --pretrained openai \
    --match-mode single_label --caption-mode both \
    --batch-size 128 --epochs 10 \
    --wandb-project mimic-cxr-clip --wandb-run-name exp7_caption_both

# Eval (substitute name and checkpoint for each):
python baseline_eval/eval_model.py \
    --model_type finetuned --name exp7_caption_single \
    --finetuned_base_model ViT-B-32 --finetuned_pretrained openai \
    --finetuned_checkpoint ./experiments/exp7_caption_single/final_merged.pt \
    --paired_dir $PAIRED_DIR --csv $TEST_CSV


================================================================================
 COMPARE ALL RESULTS SIDE-BY-SIDE
================================================================================

Add each trained model to the MODELS list in baseline_eval/run_all_evals.py,
then run:

python baseline_eval/run_all_evals.py \
    --paired_dir $PAIRED_DIR --csv $TEST_CSV \
    --output_dir ./eval_outputs/all_results \
    --batch_size 64 --skip_existing \
    --wandb-project mimic-cxr-clip --wandb-run-name all_results

Produces summary CSVs + heatmaps + bar charts in ./eval_outputs/all_results/.


================================================================================
 QUICK DIAGNOSTICS CHECKLIST
================================================================================

After each training run, check the per-epoch log line:
  [batch stats] avg_positives_per_sample=X.XX  diagonal_only=Y.Y%  conflict_pairs=Z.Z%

  avg_positives_per_sample
    ~= 1.0  -> batch too small / mode too strict -> increase --batch-size or
               switch from two_label to single_label
    > 2.0   -> healthy label-aware signal

  diagonal_only > 20%  -> same remedies as above

  conflict_pairs
    < 1%    -> --negative-weight has little effect
    5-15%   -> healthy for negative_aware mode

Training/val loss:
  val_loss rises, train_loss falls  -> overfitting; reduce --lr, --lora-r,
                                        or add --lora-dropout 0.1
  val_loss plateaus quickly         -> LR too high or rank too low
