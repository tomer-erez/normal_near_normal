here is the plan of how to train models in order to beat the benchmark after i met with moshe

1. lets exclude support devices its not a relevant label

2.i want to fine tune a clip model , not sure yet which one, definetly some of the ones from the /more_models_to_try dir, and also vanila clip and biomedclip

we will try lora fine tune on the dataset i have of the cxr, both for single labels and for mix labels like the baseline_eval/run_all_evals.py script evaluates for

so we need to create a training script that takes the training datasets @cxr_data/mimic_cxr_train.csv and allow lora fine tuning the more_models_to_try and the biomedclip and vanilla clip
then need to support running the eval models

the training should be alignment of image vs the labels. allow both single labwels and cross of two labels like the run_all_evals.py script. so sentences like: fracture, edema etc. but also if image has two lables in the csv so allow stuff like aligning the image with the sentence fracure and edema

argparse should allow us to configure important stuff